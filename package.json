{
  "name": "claude-translation-agent",
  "version": "1.0.0",
  "description": "A web page with Claude agent for text translation to Chinese",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo 'No tests specified yet' && exit 0",
    "lint": "echo 'No linter configured yet' && exit 0",
    "build": "echo 'Build completed - no build step required for this project' && exit 0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.27.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "keywords": [
    "claude",
    "translation",
    "agent",
    "chinese"
  ],
  "author": "",
  "license": "ISC"
}
